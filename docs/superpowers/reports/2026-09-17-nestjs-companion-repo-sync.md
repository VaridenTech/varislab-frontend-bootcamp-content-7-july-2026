# NestJS Companion Repository Sync Report

**Date:** 2026-09-17; prefix remediation verified 2026-09-18.
**Remote:** `git@github-variden:VaridenTech/varislab-intro-to-nestjs-v1.git`
**Source of truth:** `Week_09/01_nestjs_basic/content`

## Pre-sync snapshot

- Remote branch count: 69.
- Frozen snapshot: `/private/tmp/nestjs-companion-sync-20260917/pre-sync-refs.txt`.
- All 69 `origin/*` source refs remain identical to this snapshot. No remote mutation has occurred.

## Branch audit

Rows identify the corrected **local** target branches in the isolated clone, not the old remote-tracking refs. All 32 source lessons were audited; all 26 distinct corrected target trees were built and linted.

| Branch | Lesson title | Verified local target commit | Content check | Build/test |
| --- | --- | --- | --- | --- |
| lesson-01 | ติดตั้ง NestJS CLI | `c61e5f46b2ea88f6c86a47665b7a1968d0b28897` | PASS — Nest 12 ESM scaffold; project name corrected to iluvcoffee. | `build=0; lint=0`; `npm ci=0` |
| lesson-02 | มีอะไรอยู่ในแอป NestJS | `c61e5f46b2ea88f6c86a47665b7a1968d0b28897` | PASS — Scaffold files, AppModule, ESM imports and Vitest match. | `build=0; lint=0` reused from 01 (identical tree) |
| lesson-03 | สิ่งที่เราจะสร้างกันในคอร์สนี้ | `c61e5f46b2ea88f6c86a47665b7a1968d0b28897` | PASS — Planning only; no application delta. | `build=0; lint=0` reused from 01 (identical tree) |
| lesson-04 | เตรียมเครื่องมือ: ติดตั้ง Insomnia | `c61e5f46b2ea88f6c86a47665b7a1968d0b28897` | PASS — Insomnia setup; no source delta. | `build=0; lint=0` reused from 01 (identical tree) |
| lesson-05 | รัน NestJS ในโหมด Development | `c61e5f46b2ea88f6c86a47665b7a1968d0b28897` | PASS — Development scripts match; greeting edit is an unspecified experiment. | `build=0; lint=0` reused from 01 (identical tree) |
| lesson-06 | สร้าง Controller พื้นฐาน | `968d0da9fee0212b26709e5030d30b19f018915b` | PASS — Controller, GET /coffees and module registration match. | `build=0; lint=0` |
| lesson-07 | ใช้ Route Parameters | `bb31c0f82d53ec602f88e659b72d2b4dd8e0eb7f` | PASS — GET /coffees/:id and string parameter match. | `build=0; lint=0` |
| lesson-08 | จัดการ Request Body (Payload) | `d01b10ef7e5d86f368b6cc93383bd721f36828e7` | PASS — POST /coffees echoes JSON body as instructed. | `build=0; lint=0` |
| lesson-09 | กำหนด Response Status Codes | `d01b10ef7e5d86f368b6cc93383bd721f36828e7` | PASS — No delta matches removal of temporary 410 decorator. | `build=0; lint=0` reused from 08 (identical tree) |
| lesson-10 | จัดการคำขอ Update และ Delete | `6fb310de02b4caa3bcf6175687ce87ebf587cbae` | PASS — PATCH/DELETE handlers and responses match. | `build=0; lint=0`; lint warnings only |
| lesson-11 | ทำ Pagination ด้วย Query Parameters | `8fe9c58c4e103e9d5cfda903fe91eca2e2b568a7` | PASS — Query limit/offset handling matches. | `build=0; lint=0`; lint warnings only |
| lesson-12 | สร้าง Service พื้นฐาน | `0a4a0a4d4c05eef60f1abc8fbdf84dabc11099c6` | PASS — In-memory entity/service, delegation and DI match. | `build=0; lint=0`; lint warnings only |
| lesson-13 | ส่ง Error Message ที่เป็นมิตรกับผู้ใช้ | `4628658db060bb6176bc5fc5896387555811d774` | PASS — NotFoundException and response message match. | `build=0; lint=0`; lint warnings only |
| lesson-14 | ห่อ Business Domain ไว้ใน Modules | `ae09cc8ff3138010176847219eeec9e321d4a760` | PASS — CoffeesModule extraction and root import match. | `build=0; lint=0`; lint warnings only |
| lesson-15 | รู้จัก Data Transfer Objects (DTO) | `121ddd0eaca79ac9697d699c4a1879be388120cc` | PASS — DTOs, readonly/optional rules and service-created id match. | `build=0; lint=0`; lint warnings only |
| lesson-16 | Validate ข้อมูลขาเข้าด้วย DTO | `4bb164329cc066e85ef5337793448207914553a8` | PASS — ValidationPipe, IsString, PartialType and dependencies match. | `build=0; lint=0`; `npm ci=0`; lint warnings only |
| lesson-17 | ป้องกันข้อมูลคำขอที่ไม่พึงประสงค์ | `219ad66e1ae5e73e0e87dbefbd7c83e43c7bcbb9` | PASS — Whitelist and forbidNonWhitelisted match. | `build=0; lint=0`; lint warnings only |
| lesson-18 | แปลง Payload เป็น DTO Instance อัตโนมัติ | `8dd9cc1de97f80da852068630becec80e1d5e700` | PASS — Global transform plus numeric findOne parameter and string service-id conversion; no diagnostic logging. | `build=0; lint=0`; numeric-route runtime check=0; lint warnings only |
| lesson-19 | ก่อนเริ่มบทฐานข้อมูล: เก็บความคืบหน้าไว้ใน Branch | `1c7ccd80bf274e1eeaa4529f7195790b71be362f` | PASS — Application checkpoint retained; exact 58-lesson README introduction/map and original Nest tail. | `build=0; lint=0`; numeric-route runtime check=0; lint warnings only |
| lesson-20 | รัน PostgreSQL ด้วย Docker Compose | `569960f9c3190fb29f9074fb9efdb2faf26c1e3e` | PASS — Compose service, image, port and password match. | `build=0; lint=0`; numeric-route runtime check=0; lint warnings only |
| lesson-21 | ดูฐานข้อมูล Postgres ด้วย GUI | `569960f9c3190fb29f9074fb9efdb2faf26c1e3e` | PASS — GUI only; unchanged application/Compose tree. | `build=0; lint=0` reused from 20 (identical tree); numeric-route runtime check=0; lint warnings only |
| lesson-22 | รู้จัก Prisma และ PrismaService | `68635ab083310f80acb0943ff50cefaf95c841ab` | PASS — Prisma setup matches; CLI moved to devDependencies with regenerated lock classifications. | `build=0; lint=0`; `npm ci=0`; `generate=0`; numeric-route runtime check=0; lint warnings only |
| lesson-23 | สร้าง Prisma Model | `fbdf6a2a697b5ce70a091c91426fc8f0a64434b7` | PASS — Coffee schema, scalar flavors and retained entity match. | `build=0; lint=0`; `generate=0`; numeric-route runtime check=0; lint warnings only |
| lesson-24 | ใช้ Prisma Client เข้าถึงฐานข้อมูล | `6a24d5634febfaf7c654526eb3851819c00dbfec` | PASS — Prisma CRUD and 404 checks; string route id restored as explicitly described in HTML. | `build=0; lint=0`; `generate=0`; lint warnings only |
| lesson-25 | สร้าง Relation ระหว่างสอง Models | `3d10f7e7a31d78275f04959b6e4f685577dc3d32` | PASS — Flavor relation, unique name and temporarily commented writes match. | `build=0; lint=0`; `generate=0`; lint warnings only |
| lesson-26 | ดึง Models พร้อม Relations | `b01716ac507b462e058f12c456fa3ac47f1515ad` | PASS — Read includes and initial connectOrCreate writes match. | `build=0; lint=0`; `generate=0`; lint warnings only |
| lesson-27 | ใช้ Nested Writes สร้างและอัปเดตพร้อม Relation | `7ebf9da84dbcd9da3a065543db86b16ff2da944f` | PASS — PATCH relation replacement and write-response includes match. | `build=0; lint=0`; `generate=0`; lint warnings only |
| lesson-28 | เพิ่ม Pagination ที่ชั้นฐานข้อมูล | `72b577efa4ad707d01c6621ef01bd37453a3225c` | PASS — DTO validation, transformation and ordered skip/take match. | `build=0; lint=0`; `generate=0` |
| lesson-29 | ใช้ Transactions | `0cfb7d0817a8c8eeb58cbb91e6079abfd65c378a` | PASS — Event, recommendations, transaction callback and route match. | `build=0; lint=0`; `generate=0` |
| lesson-30 | เพิ่ม Indexes ให้ Models | `23b371b0128cf74394e3565d6c28c0710735c0c9` | PASS — Event name and name/type indexes match. | `build=0; lint=0`; `generate=0` |
| lesson-31 | ตั้งค่า Migrations ด้วย Prisma Migrate | `e10df075d8bfb89d1f35b4de7d30d803e08523a4` | PASS — Initial schema plus forward/reverse column-renaming migrations match. | `build=0; lint=0`; `generate=0` |
| lesson-32 | ควบคุม Module Encapsulation | `130aeeb54b3fa2cb951be299fb9ddaec35be2772` | PASS — Module encapsulation matches; verified local base for Task 3. | `build=0; lint=0`; `generate=0` |
| lesson-33 | ตั้งค่า ConfigModule และ Environment Files | `98231e925b901433e64e3ae90d808a6fa21941b4` | PASS — Plain `ConfigModule.forRoot()`, `dotenv/config` import removed from main.ts, `.gitignore` excludes `.env`; no namespace/`coffees.config.ts`. | `build=0; lint=0`; `generate=0`; `npm ci=0` |
| lesson-34 | ตรวจ Environment Variables ด้วย Schema | `65bfff13c30265a46d95186e23155bd563219999` | PASS — `ConfigModule.forRoot({ validationSchema: Joi.object({...}) })` matches; `joi` dependency present; no namespace/`coffees.config.ts`. | `build=0; lint=0`; `generate=0`; `npm ci=0` |
| lesson-35 | ใช้ ConfigService และจัดโครงสร้าง Configuration | `744248d31dec30fed225efe051f9b54749479862` | PASS — `app.config.ts` loaded via `load: [appConfig]`; `ConfigService` injected and reads typed `database.url`/`port` with `console.log` removed, an intentional deviation from old `origin/lesson-37` per ruling; no namespace/`coffees.config.ts`. | `build=0; lint=0`; `generate=0`; `npm ci=0`; lint warnings only (unused `databaseUrl`/`port`, matches HTML) |
| lesson-36 | Request Lifecycle และการ Bind Building Blocks | `e748870b9a9bb590ad1d05a783b11e45bc3cee5c` | PASS — `ValidationPipe` moved from `main.ts` `useGlobalPipes()` to `AppModule` provider `{ provide: APP_PIPE, useFactory: () => new ValidationPipe({...}) }` with identical options; `main.ts` no longer binds it; matches HTML exactly. Applied from `origin/lesson-39..origin/lesson-40` with no conflict. | `build=0; lint=0`; `generate=0`; lint warnings only (pre-existing unused `databaseUrl`/`port`) |
| lesson-37 | จับ Exceptions ด้วย Filters | `b65c2fc2d0ecb7b8b905ca08b3ac8319ba549aba` | PASS — `src/common/filters/http-exception.filter.ts` created with `@Catch(HttpException)`, `getStatus()`/`getResponse()` spread plus `timestamp`, including the documented `typeof response === 'string'` quirk; registered via `app.useGlobalFilters(new HttpExceptionFilter())` in `main.ts`; matches HTML exactly. Applied from `origin/lesson-40..origin/lesson-41` with no conflict. | `build=0; lint=0`; `generate=0`; lint warnings only (pre-existing) |
| lesson-38 | สร้าง Guard และเปิด Public Routes ด้วย Metadata | `b650c7d672e8fecb9c28b18f22bf411b8e21ef39` | PASS — combined lesson verified for both halves: (1) guards — `src/common/guards/api-key.guard.ts` injects `Reflector`+`ConfigService`, reads `IS_PUBLIC_KEY` via `context.getHandler()`, falls back to `configService.get('API_KEY')`; (2) public-route metadata — `src/common/decorators/public.decorator.ts` (`SetMetadata(IS_PUBLIC_KEY, true)`), `@Public()` applied to `findAll()` in `coffees.controller.ts`, guard registered via `{ provide: APP_GUARD, useClass: ApiKeyGuard }` in new `common.module.ts`. `coffees.service.ts` untouched — the ruling-mandated typed `get<string>`/`get<number>` pattern (no `console.log`) remained intact throughout this range; this diff range did not touch that file, so no conflict occurred. Applied from `origin/lesson-41..origin/lesson-43` with no conflict. | `build=0; lint=0`; `generate=0`; lint warnings only (pre-existing) |
| lesson-39 | สร้าง Interceptors สำหรับ Response และ Timeout | `866114f14ec560e5aef69a19f752e947d9738b22` | PASS — combined lesson verified for both halves: (1) `src/common/interceptors/wrap-response.interceptor.ts` wraps output in `{ data }` via `map()`, retains `console.log('Before...')`; (2) `src/common/interceptors/timeout.interceptor.ts` applies `timeout(3000)` + `catchError` converting `TimeoutError` to `RequestTimeoutException`, using the modern `throwError(() => ...)` form the HTML's warning note calls out as current best practice; both registered together via `app.useGlobalInterceptors(new WrapResponseInterceptor(), new TimeoutInterceptor())`; no leftover temporary 5s delay in `findAll()`. Applied from `origin/lesson-43..origin/lesson-45` with no conflict. | `build=0; lint=0`; `generate=0`; lint warnings only (pre-existing) |
| lesson-40 | สร้าง Custom Pipes | `bdafcb689be0da0a5bee235d2ab9c1a4644c34cf` | PASS — `src/common/pipes/parse-int.pipe.ts` implements `PipeTransform`, `parseInt`/`isNaN` check, throws `BadRequestException` with matching message; bound at parameter level via `@Param('id', ParseIntPipe) id: number` in `findOne()`, service still called with `'' + id`; matches HTML exactly. Applied from `origin/lesson-45..origin/lesson-46` with no conflict. | `build=0; lint=0`; `generate=0`; lint warnings only (new: unused `metadata` param in the custom pipe, expected — matches HTML signature) |
| lesson-41 | โบนัส: เพิ่ม Request Logging ด้วย Middleware | `3886d5c37c150c6dacddb07e7a4f7a886aef82bc` | PASS — `src/common/middleware/logging.middleware.ts` implements `NestMiddleware`, `console.time`/`console.timeEnd` keyed off the `finish` event, calls `next()`; `common.module.ts` now `implements NestModule` with `configure(consumer) { consumer.apply(LoggingMiddleware).forRoutes('*'); }`; matches HTML exactly. Applied from `origin/lesson-46..origin/lesson-47` with no conflict. | `build=0; lint=0`; `generate=0`; lint warnings only (pre-existing) |
| lesson-42 | โบนัส: สร้าง Custom Param Decorators | `5f7910f79358748d2b11258a1bd75cfbc3c269c7` | PASS — `src/common/decorators/protocol.decorator.ts` uses `createParamDecorator((data: unknown, ctx) => ctx.switchToHttp().getRequest().protocol)`; `@Protocol()` added alongside `@Public()`/`@Query()` in `findAll()` with a `console.log(protocol)`; matches HTML exactly. Applied from `origin/lesson-47..origin/lesson-48` with no conflict. | `build=0; lint=0`; `generate=0`; lint warnings only (pre-existing) |
| lesson-43 | ตั้งค่า Swagger และเปิดใช้ CLI Plugin | `2de5c90d109097ea4f75e382bcf85b5008b50839` | PASS — both halves verified: `nest-cli.json` has `"plugins": ["@nestjs/swagger/plugin"]`; `main.ts` has `DocumentBuilder().setTitle('Iluvcoffee').setDescription('Coffee application').setVersion('1.0')`, `SwaggerModule.createDocument`, `SwaggerModule.setup('api', ...)` before `app.listen()` — matches HTML `43_setting-up-swagger-and-cli-plugin.html` verbatim. Applied from `origin/lesson-48..origin/lesson-50`, clean, no conflict. | `npm ci=0` (lockfile changed); `build=0; lint=0` |
| lesson-44 | ตกแต่ง Model Properties ให้เอกสารครบ | `0b40791778a7d7145c14bacbd2749ca67e96b0cb` | PASS — `create-coffee.dto.ts` gets `@ApiProperty({ description: ... })` on `name`/`brand` and `@ApiProperty({ example: ['chocolate','vanilla'] })` on `flavors`; `update-coffee.dto.ts`'s `PartialType` import switched from `@nestjs/mapped-types` to `@nestjs/swagger` (required so `@ApiProperty` metadata survives on the partial). Matches HTML exactly. Applied from `origin/lesson-50..origin/lesson-51`, clean, no conflict. | `build=0; lint=0` |
| lesson-45 | อธิบาย Responses และจัดกลุ่ม API ด้วย Tags | `af1b9c8229127b56763483e05e0b393ead6dd3dd` | PASS — both halves verified in `coffees.controller.ts`: response docs — `@ApiNotFoundResponse({ description: 'Coffee not found.' })` on `findOne`, `@ApiForbiddenResponse({ description: 'Forbidden.' })` on `create` (not `findAll`, which stays `@Public()` per lesson 38); tag grouping — `@ApiTags('coffees')` on the controller class. Matches HTML `45_documenting-responses-and-grouping-with-tags.html` exactly (only 4 insertions, confirmed sufficient — the lesson adds three decorators plus one import line, nothing else). Applied from `origin/lesson-51..origin/lesson-53`, clean, no conflict. | `build=0; lint=0` |
| lesson-46 | เริ่มต้น Vitest และสร้าง Test Suite | `afda2b6660bf89ff14f5c98751de77f254eccf34` | PASS, with one resolved conflict — see "Rebuilt Swagger and testing lessons 43–50" below. Both halves verified: Vitest startup half is satisfied by the pre-existing scaffold from lesson-01 (`vitest.config.ts`/`vitest.config.e2e.ts` and the `test`/`test:e2e` scripts were added when the CLI first scaffolded the Nest 12 project, and lesson-46's own HTML explicitly frames this as "since Nest 12, `nest new` already sets up Vitest" rather than something this lesson introduces); test-suite half is satisfied by real spec additions to `coffee-rating.service.spec.ts`, `coffees.controller.spec.ts`, and `coffees.service.spec.ts`. Applied from `origin/lesson-53..origin/lesson-55`. | `build=0; lint=0; test=0` (4 test files, 4 tests) |
| lesson-47 | เขียน Unit Tests | `86e0a8977ddc49ab19bd75175a22ab6b8c7d7101` | PASS — additional cases added to `coffees.service.spec.ts` only; matches HTML `47_adding-unit-tests.html`. Applied from `origin/lesson-55..origin/lesson-56`, clean, no conflict. | `build=0; lint=0; test=0` (4 test files, 6 tests) |
| lesson-48 | เจาะลึก e2e Tests | `d7af3bcba168a30ff14fef521a332d03e1115490` | PASS — `test/app.e2e-spec.ts` switched `beforeEach`/`afterEach` to `beforeAll`/`afterAll` (expensive app boot once per suite) and added `.set('Authorization', process.env.API_KEY!)` to satisfy the `ApiKeyGuard` added in lesson-38; matches HTML `48_diving-into-e2e-tests.html` exactly. Applied from `origin/lesson-56..origin/lesson-57`, clean, no conflict. | `build=0; lint=0; test=0` (4 test files, 6 tests) |
| lesson-49 | สร้าง e2e Test แรก | `5b9dffe43bdf26b71e9aba5266063ab0396ba7df` | PASS — `docker-compose.yml` gains a `test-db` service on host port 5433; `package.json` gains exactly the three scripts `pretest:e2e`/`test:e2e`/`posttest:e2e` (confirmed via `git diff lesson-48 lesson-49 -- package.json`, no other drift); new `test/coffees/coffees.e2e-spec.ts` sets `process.env.DATABASE_URL` at top-of-file (pointing at the 5433 test-db, since the file imports `CoffeesModule`/`PrismaModule` directly with no `ConfigModule`, so nothing else will supply it) with `it.todo` stubs for the five CRUD cases; matches HTML `49_creating-our-first-e2e-test.html` exactly. Applied from `origin/lesson-57..origin/lesson-58`, clean, no conflict (one hunk fell back from three-way to direct application on `package.json`, which is expected when a line has no merge-base blob, and produced no conflict markers). | `build=0; lint=0; test=0` (4 test files, 6 tests) |
| lesson-50 | เขียน Logic ของ e2e Test | `a154103709aa1b1323c3d3197865c231c20b47da` | PASS — `test/coffees/coffees.e2e-spec.ts`'s five `it.todo` stubs are implemented with real `supertest` requests (`POST`/`GET`/`GET :id`/`PATCH`/`DELETE`), a global `ValidationPipe` matching `main.ts`'s options, and an `expectedPartialCoffee` matcher; matches HTML `50_implementing-e2e-test-logic.html`. Applied from `origin/lesson-58..origin/lesson-59`, clean, no conflict. | `build=0; lint=0; test=0` (4 test files, 6 tests); e2e lifecycle below |
| lesson-51 | ก่อนเริ่มบท MongoDB: กลับไปที่จุดจบ Chapter 2 | `1e91f92e69e118822e0e21add7d7e60b55dfff50` | PASS — forked `rebuild/mongodb`/`lesson-51` from the corrected LOCAL `end-of-chapter-2`/`lesson-18` checkpoint (`8dd9cc1de97f80da852068630becec80e1d5e700`, not the stale `origin/end-of-chapter-2` at `c92dfb93e7f46e3b0b5c148b7a4c7015695b2556`), per Task 2's binding downstream ruling; replaced only `README.md` with the current `lesson-33:README.md` 58-lesson text (identical to `lesson-19`'s README — unchanged between lesson-19 and lesson-33 — and confirmed it lists `lesson-51`…`lesson-58` as the MongoDB path based on `end-of-chapter-2`) and committed. `git diff --name-only end-of-chapter-2 lesson-51` prints only `README.md`; `git diff --exit-code end-of-chapter-2 lesson-51 -- . ':!README.md'` exits 0 — source tree (in-memory `coffees`, no Prisma) is byte-identical to the checkpoint. Matches HTML `51_before-we-get-started-mongodb.html` (checkpoint return, README-only change, no MongoDB/Prisma content introduced — that starts at lesson-52; HTML line 55 also asserts no `src/generated/` folder, confirmed below). | `build=0; lint=0`; absence checks: no `prisma/` dir, no `src/prisma/` dir, no `src/generated/` dir (HTML-required, brief omitted it — checked directly), no `@prisma/client`/`@nestjs/mongoose`/`mongoose` in `package.json`; lint warnings only (pre-existing, matches lesson-18); working tree clean after build/lint (`tsconfig.build.tsbuildinfo` restored to committed state) |
| lesson-52 | รัน MongoDB ด้วย Docker Compose | `d368c798d7e28428d4eadb2f8c0c80994548e441` | PASS — `docker-compose.yml` gains only the `db` service (`image: mongo:8.0`, `ports: 27017:27017`, `MONGO_INITDB_DATABASE: nest-course`); matches HTML `52_running-mongodb.html` exactly, including the pinned Major-Release tag and the documented `SERVER-121912` kernel-incompatibility warning. Applied from `origin/lesson-60..origin/lesson-61`, clean, no conflict (diff byte-identical to the naive range diff). | `build=0; lint=0` (2 pre-existing unused-param warnings); live check: `mongo:8.0` failed to start on this machine with the exact documented `SERVER-121912` kernel error, `mongo:8.0.4` (uncommitted local override) started cleanly — confirms the HTML's fallback guidance |
| lesson-53 | รู้จัก Mongoose Module | `b046ccf73b6052afabb9cd387482b9b8d8a95146` | PASS — `package.json`/`package-lock.json` gain `@nestjs/mongoose`+`mongoose`; `app.module.ts` adds `MongooseModule.forRoot('mongodb://localhost:27017/nest-course')` alongside `CoffeesModule`; matches HTML `53_introducing-the-mongoose-module.html`. Applied from `origin/lesson-61..origin/lesson-62`, clean, no conflict (diff content identical to the naive range diff; only blob `index` hashes differed, as expected from unrelated surrounding-file drift). | `npm ci=0` (lockfile changed); `build=0; lint=0` (2 pre-existing warnings) |
| lesson-54 | สร้าง Mongoose Model | `e54c0ab922c9b1ef7ce66028427ab9a6bdd69ae4` | PASS — `coffee.entity.ts` converted to `@Schema()`/`extends Document` with `@Prop()` fields and `SchemaFactory.createForClass`; `coffees.module.ts` registers `MongooseModule.forFeature([{ name: Coffee.name, schema: CoffeeSchema }])`; matches HTML `54_creating-a-mongoose-model.html` exactly, including the explicit expectation (HTML line 79) that `CoffeesService` will now fail to compile against the old in-memory array until the next lesson rewrites it. Applied from `origin/lesson-62..origin/lesson-63`, clean, no conflict (diff byte-identical to naive range diff). | `build=1` (expected — TS2353/TS2339/TS2345 on `coffees.service.ts`'s in-memory `id`/array usage against the new `Coffee extends Document` type, exactly as HTML line 79 says will happen); `lint=0` (2 pre-existing warnings) |
| lesson-55 | ใช้ Mongoose Model เข้าถึง MongoDB | `6eb6ed798a4036bee40dcc71c30a5fd8fa69fcf9` | **PASS** — `coffees.service.ts` correctly rewritten to `@InjectModel(Coffee.name)` with `.find()/.findOne()/.findOneAndUpdate()/.deleteOne()` (not the removed `.remove()`), matching HTML `55_using-a-mongoose-model-to-access-mongodb.html`. HTML line 82 instructs removing any numeric id coercion from the controller ("ถ้า controller ของคุณเคยแปลง id เป็น number ... ต้องถอดออก"); the initial rebuild missed this (the fix predates this diff range and was never expressed as a hunk in any of the seven `origin/lesson-60..67` diffs, since the local `lesson-51` base was forked from the lesson-18 checkpoint rather than an already-corrected upstream state). Fixed by amending this commit's `coffees.controller.ts`: `findOne(@Param('id') id: string) { return this.coffeesService.findOne(id); }`. Applied from `origin/lesson-63..origin/lesson-64` plus this fix; verified the amendment changes only `coffees.controller.ts` (and expected `tsconfig.build.tsbuildinfo` churn) relative to the prior SHA `867b959c280c6cb450e8880c548da60932f31e66`. | `build=0; lint=0` (1 pre-existing warning); live: `POST /coffees`=201, `GET /coffees/:id`=**200** with the real Mongo `_id` (re-verified against a fresh `mongo:8.0.4` container after the fix) |
| lesson-56 | เพิ่ม Pagination (MongoDB) | `288a38719f3a26851d98a48f474484686a6e92fd` | **PASS** — `pagination-query.dto.ts` created (`limit`/`offset`, `@Type(()=>Number)`, `@IsPositive`/`@IsOptional`), `coffees.controller.ts`/`coffees.service.ts` wire `@Query() paginationQuery: PaginationQueryDto` through to `.skip(offset).limit(limit)`; matches HTML `56_adding-pagination-mongodb.html` exactly. Rebased onto the corrected lesson-55 (was `c1dd51611fc99a7ec005590cf7ae28284609b411`); only conflict during rebase was the generated `tsconfig.build.tsbuildinfo`, resolved by taking the incoming version (regenerated by the next build regardless) — `coffees.controller.ts`/`.service.ts` auto-merged cleanly since this range's diff never touched `findOne`. `GET /coffees/:id` now 200 (inherits the lesson-55 fix). | `build=0; lint=0` (0 warnings — pagination DTO uses its params, clearing the earlier unused-param warning) |
| lesson-57 | ใช้ Transactions (MongoDB) | `e87696dc3aa42ebe689ea0233d92082c8ea3eab5` | **PASS** — `events/entities/event.entity.ts` created (`@Prop({ type: mongoose.SchemaTypes.Mixed })` for `payload`, correctly braced — not the bare form that throws `CannotDetermineTypeError`); `coffees.service.ts` adds `@InjectConnection() connection: Connection` typed via `import type { Connection, Model } from 'mongoose'` (ESM-safe) and `recommendCoffee()` uses `connection.startSession()`/`startTransaction()`/`commitTransaction()`/`abortTransaction()`; `coffee.entity.ts` gains `recommendations`; `coffees.controller.ts` adds `POST :id/recommend`. Matches HTML `57_use-transactions-mongodb.html`, including its documented standalone-vs-replica-set caveat (HTML line 90) — live-tested on a standalone `mongo:8.0.4` container, and the write was correctly aborted (`recommendations` stayed `0` in `mongosh`), matching the "all-or-nothing" behavior the HTML predicts for a non-replica-set container. Rebased cleanly onto the corrected lesson-56 (was `75839e8ab63aa3892017eadfdb79172ccc41ffb1`), no conflicts — inherits the `findOne` id-type fix; `GET /coffees/:id` now 200. | `build=0; lint=0` (1 pre-existing warning); live: standalone-transaction abort behavior confirmed via `mongosh` (recommendations remained `0`) |
| lesson-58 | เพิ่ม Indexes ให้ Schemas | `92346819b77cc64d27f220cc3776f93af46d37f3` | **PASS** — `event.entity.ts` gains `@Prop({ index: true })` on `name` and `EventSchema.index({ name: 1, type: -1 })`; matches HTML `58_adding-indexes-to-schemas.html` exactly. Rebased cleanly onto the corrected lesson-57 (was `36a9c3ddae511283e094e5afdc00121f8c18677c`), no conflicts. This is `rebuild/mongodb`'s current `HEAD`. Isolation checks (Step 3 of the brief) all pass: `@nestjs/mongoose` present only here (`package.json`, `app.module.ts`, `coffees.module.ts`, `coffees.service.ts`, both entity files); `@prisma/client` absent; `events/entities/event.entity.ts` exists only on this lineage and is absent from `lesson-50` (SQL path), which itself still has `prisma/schema.prisma`. Re-verified `git diff` against the pre-fix SHA touches only `coffees.controller.ts` (the id-type fix). | `build=0; lint=0` (1 pre-existing warning); isolation grep/cat-file checks all passed; live: `POST /coffees`=201, `GET /coffees/:id`=**200** re-confirmed on this exact commit after a fresh `npm run build` + app start against a fresh `mongo:8.0.4` container |

## Building-block lessons 36–42: PASS

All seven `apply_lesson` calls from the plan's exact function/loop in Task 4 completed with `git apply --index --3way` applying cleanly (some fell back from three-way to direct application when no merge base blob existed for a new file, which is expected and not a conflict — none produced conflict markers). **No conflict occurred in `coffees.service.ts`**: none of the seven old-lesson diff ranges (`origin/lesson-39..40`, `40..41`, `41..43`, `43..45`, `45..46`, `46..47`, `47..48`) touched that file (`git diff --stat lesson-35 lesson-42 -- src/coffees/coffees.service.ts` is empty), so the lesson-35 ruling-mandated typed `get<string>('database.url')`/`get<number>('port', 3000)` pattern with no `console.log` remained untouched and was reconfirmed present at lesson-42.

Namespace verification (Step 3) passed for lesson-36 through lesson-42: no `src/coffees/coffees.config.ts` blob, and no matches for `registerAs(`, `ConfigModule.forFeature`, `coffeesConfig`, or `coffees.foo` anywhere in `src` on any of the seven branches.

Both combined lessons were explicitly checked for both halves: lesson-38 contains both the `ApiKeyGuard` (guards half) and the `@Public()` decorator plus `APP_GUARD` registration (public-route metadata half); lesson-39 contains both `WrapResponseInterceptor` (response half) and `TimeoutInterceptor` (timeout half), both registered together in `main.ts`.

`tsconfig.build.tsbuildinfo` is gitignored (confirmed via `.gitignore` containing `*.tsbuildinfo` and `git ls-files` returning no match), consistent with Task 3's finding that this became untracked from lesson-32 onward — the restore step was a no-op on every one of the seven branches; `git status --short` was clean after every build.

## Repository-wide checks

### Corrected lessons 01–32: PASS

The initial audit found stale README numbering, project-name metadata, Prisma dependency classification, and the missing lesson-18 primitive-conversion state. The recorded ruling authorized rebuilding the prefix locally. All four findings are resolved; Task 3 and Task 6 must consume the corrected local refs below.

- `lesson-32`: **130aeeb54b3fa2cb951be299fb9ddaec35be2772**; tree **19d4ea46e4d008fca327de4d74a68bb86bd1a7ab**.
- `end-of-chapter-2` and `lesson-18`: both **8dd9cc1de97f80da852068630becec80e1d5e700**; both trees **97cd998ab4bf2a02f17630e8690507ed52abf02c**.
- `rebuild/prefix` points to local `lesson-32`. Each numbered local ref is an ancestor of the next. Identical checkpoints share commits: **01–05**, **08–09**, **20–21**.
- The original `origin/lesson-32` and `origin/end-of-chapter-2` are frozen historical sources, not downstream target bases.

### Corrections and source fidelity

1. **Lesson 01 / carried through 32:** package.json and both package-lock project-name fields now say `iluvcoffee`, matching `nest new iluvcoffee`. Initial scaffold timing remains the established 01–05 baseline.
2. **Lesson 18 / carried through 23:** `findOne(@Param('id') id: number)` calls `this.coffeesService.findOne('' + id)`; global `transform: true` remains enabled. No temporary logging remains. Lesson 24 explicitly describes string route ids, so its controller returns to `id: string` with direct service delegation.
3. **Lesson 19 / carried through 32:** README introduction and branch map exactly match the plan's 58-lesson text. The original Nest-generated content below the horizontal rule is preserved byte-for-byte.
4. **Lesson 22 / carried through 32:** `prisma: "^7.10.0"` is in devDependencies, absent from dependencies, and the lockfile root agrees. `npm install --package-lock-only --ignore-scripts --save-dev prisma@7.10.0` exited 0 and updated 131 package classification flags. The locked package set, versions, integrity values and all other package-entry data are unchanged.
5. Old lesson deltas were replayed sequentially with `git diff --binary origin/lesson-PP origin/lesson-NN | git apply --index --3way`. All other tracked content equals the previously audited original tree for its lesson. Full comparisons permit only the specific name, README, dependency-classification and 18–23 controller corrections above.

### Fresh verification of corrected local refs

Environment: Node `v26.7.0`, npm `11.19.0`; all commands ran in `/private/tmp/nestjs-companion-sync-20260917/repo`. Table shorthand: `build` = `npm run build`; `lint` = `npm run lint`; `generate` = `npx --no-install prisma generate`. Numbers are exit codes.

- `node /private/tmp/nestjs-companion-sync-20260917/verify-rebuilt-prefix.mjs`: **exit 0**. Checked all 32 corrected trees, full allowed-change scope, package/lock agreement, exact README, sequential ancestry, lesson-18/checkpoint commit and tree equality, all 69 frozen origin refs, and clean status.
- `node /private/tmp/nestjs-companion-sync-20260917/verify-prefix-builds.mjs`: **exit 0**. Built/linted all **26 distinct local trees**, verified emitted `dist/main.js` for each, and recorded every command/status. Only identical trees were skipped.
- `npm ci`: **exit 0** at local **01, 16, 22**, whenever the lockfile changed. Dependencies were retained between switches.
- `npm run build` and `npm run lint`: **exit 0** on every distinct local snapshot. For every local lesson **22–32**, `npx --no-install prisma generate` ran first and exited **0**.
- Runtime primitive-conversion checks: **exit 0** for each distinct state in **18–23** (21 reuses 20). Compiled decorator metadata is Number; ValidationPipe transforms URL `'42'` into numeric `42`; the controller delegates string `'42'` to the service.
- `git status --short` was recorded after every distinct build/lint. Lesson 01 generated only untracked tsbuildinfo, preserved outside the clone as `audit/rebuilt-lesson-01.generated.tsbuildinfo`. Lessons 18–20 changed only tracked tsbuildinfo; `git restore tsconfig.build.tsbuildinfo` restored it. Other tracked snapshots 06–21 were clean and their tracked tsbuildinfo was also restored. From 22 the file is ignored/untracked. No other tracked file changed.
- After all final checks, `git clean -ndX` listed only dist/, node_modules/, src/generated/ and tsconfig.build.tsbuildinfo. Authorized `git clean -fdX`: **exit 0**, removing those reproducible outputs only in the isolated clone. Final `git status --short` and `git clean -ndX` were empty. Content/ref verification was rerun successfully after cleanup.

### Remaining concerns and verification limits

- Existing unused-parameter lint warnings remain in lessons 10–27, with exit 0; these match the instructional intermediate code.
- npm's install audit reported **7 vulnerabilities (2 low, 1 moderate, 4 high)** at 01/16 and **11 (2 low, 1 moderate, 8 high)** at 22. Dependency versions were preserved to match the course; no audit-fix upgrade was applied. npm also reported deprecated tsconfck and install scripts awaiting approval; documented client generation and every build still succeeded.
- No database/container mutations, HTTP smoke tests, full unit suites or e2e suites were run for this prefix task. Verification covers the requested static content/build/lint checks plus the specific corrected parameter-conversion behavior.

### Evidence

The initial audit and its pre/post-generation results remain in the task-2 handoff and Git history. Initial original-tree builds 22–32 failed on missing generated Prisma Client, then passed after the documented generation step; corrected local builds ran generation before building and all passed on their first verification pass.

Raw evidence: `/private/tmp/nestjs-companion-sync-20260917/audit/`. Key files: `rebuilt-mapping.json` (full old-to-local mapping and trees), `rebuilt-build-results.json` (all corrected commands, outputs and exit statuses), `replay-commands.json`, `meta.json`, `lesson-NN.txt`, `lesson-NN.full.diff`, `build-results.json`, and `generated-build-results.json`.

### Rebuilt Swagger and testing lessons 43–50: PASS

Continuing on `rebuild/sql` from the reviewed `lesson-42` (`5f7910f79358748d2b11258a1bd75cfbc3c269c7`), the eight exact old-diff ranges in the task table were applied and committed sequentially with `git diff --binary "$OLD1" "$OLD2" | git apply --index --3way`, `git commit`, `git branch -f`. The eight resulting rows (lesson-43 through lesson-50) are appended directly after the lesson-42 row in the **Branch audit** table above, in the same 5-column format as every other row.

**Conflict resolution (lesson-46, `src/coffees/coffees.service.ts`):** the old diff range `origin/lesson-53..origin/lesson-55` assumes a "before" state with the debug `coffeesConfig`/`@Inject(coffeesConfig.KEY)` scaffolding and untyped `configService.get('database.url')` + `console.log` calls (removed earlier, at lesson-35/lesson-42, per Task 3/4's ruling, which instead left typed `get<string>('database.url')`/`get<number>('port', 3000)` with no `console.log`). `git apply --3way` conflicted on both the import list and the constructor body because the context lines didn't match. Resolution: lesson-46's own HTML (`46_getting-started-with-vitest-and-test-suites.html`, line 98) explicitly instructs "เก็บกวาด constructor ก่อนเริ่มบทนี้: ...วิธีที่ตรงกับโค้ดในบทนี้ที่สุดคือลบ injection และ log ที่ใช้ทดลองออก ให้ constructor เหลือ `constructor(private readonly prisma: PrismaService) {}`" — i.e. the lesson's own planned content is to remove `ConfigService` from `CoffeesService` entirely (it's dead debug code the test-module setup would otherwise need a mock for), not merely to detype it. This is not a reversion of the lesson-35/Task-4 ruling — that ruling only says the earlier *typed* pattern must not be reverted to the old *untyped* pattern; here the whole block is deliberately deleted per the lesson's own text, which the old upstream diff also does (confirmed the resolved file is byte-identical to `origin/lesson-55:src/coffees/coffees.service.ts`). Resolved by taking the "theirs" side for both hunks (dropping the `ConfigService` import and the two-line typed body), leaving `constructor(private readonly prisma: PrismaService) {}`. Rebuild confirmed the two `no-unused-vars` lint warnings for `databaseUrl`/`port` present at lesson-43 through lesson-45 disappear starting at lesson-46, as expected.

**Apply-loop recovery note:** the brief's `apply_lesson` bash function (wrapped in `set -euo pipefail`) did not abort the outer script when `git commit` failed on the lesson-46 conflict — subsequent `apply_lesson` calls for lesson-47 through lesson-50 kept running, staging their patches on top of the still-unmerged `coffees.service.ts` and force-setting `lesson-47`..`lesson-50` to the pre-conflict `HEAD` (`af1b9c8`, i.e. lesson-45). This was caught before committing anything further: `git merge --abort` (no-op, no `MERGE_HEAD`), `git reset --hard af1b9c8`, all five branch pointers reset to `af1b9c8`, then lessons 46–50 were reapplied one at a time with the conflict resolved as above before each was committed and branched individually. No incorrect commits were left in the branch/ref list.

**Progressive validation (Step 3):** `package-lock.json` changed only at lesson-43 (Swagger deps); `npm ci` ran once there. `npm run build`/`npm run lint` exited 0 on every branch 43–50; `npm test` exited 0 on every branch 46–50 (test counts shown in the table above). `tsconfig.build.tsbuildinfo` remains gitignored (`*.tsbuildinfo` in `.gitignore`, `git ls-files` returns no match) — confirmed still true, consistent with Task 3/4's finding that this became untracked from lesson-32 onward; no restore was ever needed, and `git status --short` was clean after every build.

**e2e lifecycle (lesson-50):** the isolated clone has no `.env` file (it is gitignored and, per Task 3's audit, never tracked on any branch) and the companion repo's own `pretest:e2e` script only exports `DATABASE_URL` inline for its own `prisma migrate deploy` invocation, not for the app boot in `npm run test:e2e` itself. First attempt without a `.env`: `npm run pretest:e2e` exit 0 (test-db up, migrations applied), `npm run test:e2e` exit 1 (`test/app.e2e-spec.ts` failed with `Config validation error: DATABASE_URL: "DATABASE_URL" is required`, since it boots the full `AppModule` including `ConfigModule.forRoot()`'s Joi validation — `test/coffees/coffees.e2e-spec.ts` passed, since it sets `process.env.DATABASE_URL` itself at the top of the file), `npm run posttest:e2e` exit 0 (test-db container removed cleanly). Diagnosed as a harness/environment gap, not a defect in the applied commits: created a temporary, untracked `.env` (`DATABASE_URL=postgresql://postgres:pass123@localhost:5433/postgres` pointing at the task's own `test-db`, never the unrelated `iluvcofee-db-1` container on port 5432; `PORT=3000`; `API_KEY=iluvcoffeeSecretKey`, the exact value lesson-38's HTML documents), matching what the course instructs a student to have configured locally by this point. Reran the full lifecycle: `npm run pretest:e2e` exit 0, `npm run test:e2e` exit 0 (2 test files, 6 tests passed), `npm run posttest:e2e` exit 0. Deleted the temporary `.env` afterward; `git status --short` and `docker ps` both returned to the pre-task baseline (only the pre-existing unrelated `ecommerce-api-db-1`, `iluvcofee-db-1`, `mongo-rs`, `mongo-standalone` containers remained; `repo-test-db-1` was created and removed cleanly both times).

**Step 2 — main:** `git branch -f main lesson-50` then `test "$(git rev-parse main^{tree})" = "$(git rev-parse lesson-50^{tree})"` — **PASS**, both point at tree of commit `a154103709aa1b1323c3d3197865c231c20b47da`.

**Step 4 — content boundaries:** `test ! -e src/coffees/coffees.config.ts` — **PASS** (absent, confirmed removed at lesson-35, never reintroduced by lessons 43–50). `grep -rnE "registerAs\(|ConfigModule\.forFeature|coffeesConfig|coffees\.foo" src` (`rg` equivalent match set — `rg` is a zsh shell function in this environment that isn't defined inside a `bash -c` subshell, so `grep -E` was used instead) — **PASS**, no matches. `git diff --exit-code main lesson-50` — **PASS**, exit 0, empty diff. `git diff lesson-50 origin/lesson-59` (informational, not a required check) shows only expected pre-existing divergence carried from Tasks 1–4: README text (67-lesson intro vs. old 58-lesson intro), `package-lock.json` (different dependency set from the already-diverged local lineage), and `src/coffees/coffees.config.ts`/`coffees.module.ts` (old upstream's final state never actually deleted the standalone config file/module registration — a leftover the local branch already cleaned up at lesson-35, well before this task).

Evidence: `/private/tmp/nestjs-companion-sync-20260917/repo` (branches `lesson-43`..`lesson-50`, `main`); build/lint/test logs saved under this session's scratchpad at `task5-logs/`.

### Rebuilt MongoDB lessons 52–58: PASS (one defect found and fixed, lessons 55–58)

The seven commits (`lesson-52` through `lesson-58`, `rebuild/mongodb` at `HEAD` = `lesson-58`) were already created and branched by a prior implementer session before it was terminated mid-task; this pass verified them rather than recreating them. Ancestry confirmed sequential (`lesson-N^` = `lesson-(N-1)` for all seven, down to `lesson-52^` = `lesson-51`); companion working tree was clean at the start (`tsconfig.build.tsbuildinfo` tracked on this lineage, unlike the SQL lineage from lesson-32 onward).

**Clean-apply verification:** for each of the seven commits, `git diff --binary <old-from> <old-to> -- . ':!tsconfig.build.tsbuildinfo'` (the naive range diff) was compared byte-for-byte against `git diff --binary lesson-(N-1) lesson-N -- . ':!tsconfig.build.tsbuildinfo'` (the actual committed diff). Five of seven (`lesson-52`, `54`, `55`, `58`, and — after accounting for an initial `tail -5` truncation artifact in the first pass — also `56`/`57`) were byte-identical. `lesson-53`, `56`, `57` differed only in the diff header's `index <blob>..<blob>` lines (pre/post-image blob hashes, which shift when unrelated surrounding file content differs slightly), never in any `+`/`-` content or context line. No conflict markers (`<<<<<<<`/`=======`/`>>>>>>>`) were found in any of the seven commits via `git show | grep`. Conclusion: all seven `git apply --3way` calls applied cleanly with no conflict resolution — a materially different outcome from the SQL lineage's lesson-46, which did require one.

**Domain-fact spot checks (all confirmed correct in the committed code):** `coffees.service.ts` uses `.deleteOne()`, never the removed `Document#remove()`; the `recommendCoffee` payload uses `coffee._id`, never `.id`; `coffees.service.ts` imports `Connection`/`Model` via `import type { ... } from 'mongoose'` (ESM-safe); `event.entity.ts`'s `payload` field uses `@Prop({ type: mongoose.SchemaTypes.Mixed })` (braced form), not the bare form that throws `CannotDetermineTypeError`; `docker-compose.yml` at lesson-52 pins `image: mongo:8.0`, matching the HTML exactly; `lesson-51`'s `coffees.service.ts` had no `ConfigService`/`COFFEE_BRANDS` injection to strip, and none of lessons 52–58 reintroduced any.

**Defect found (live-tested, not caught by build/lint) — since fixed, see below:** `coffees.controller.ts`'s `findOne` route (`GET /coffees/:id`) originally read `@Param('id') id: number)` / `this.coffeesService.findOne('' + id)`, unchanged since `lesson-51`. `lesson-55`'s own HTML (`55_using-a-mongoose-model-to-access-mongodb.html`, line 82) explicitly instructs removing exactly this kind of numeric id coercion ("ถ้า controller ของคุณเคยแปลง id เป็น number ... ต้องถอดออก ไม่อย่างนั้น query จะหาไม่เจอทุกครั้ง"), but the change was never made in the initial rebuild. Confirmed live on the original `lesson-58` (`36a9c3d`, pre-fix): with a `mongo:8.0.4` container up, `POST /coffees` → `201` with a real Mongo `_id`, then `GET /coffees/:id` on that same id → `500 {"statusCode":500,"message":"Internal server error"}`. Cause: `main.ts`'s global `ValidationPipe({ transform: true })` coerces the route param via `Number(id)`, and a 24-char hex ObjectId string is not numeric, so `Number(id)` = `NaN`, then `'' + NaN` = `"NaN"`, and `coffeeModel.findOne({ _id: "NaN" })` throws an uncaught Mongoose `CastError` — never caught by the controller/service, surfaced as a bare 500 with no server-side log line (Nest's default filter didn't print a stack trace for it either). The break window is lessons 55–58 (52–54 predate the Mongoose-backed service, so the same code path only ever compares against in-memory numeric ids there and is inert).

Root-caused as a **base-state gap that predates this task**, not a bad merge: `git diff --stat lesson-51 origin/lesson-60 -- . ':!README.md' ':!package-lock.json'` shows exactly two lines differ — `package.json`'s cosmetic `"name"` field (`iluvcoffee` vs `iluvcoffee-new`, irrelevant, pre-existing lineage-naming drift) and this same `coffees.controller.ts` hunk, already fixed on the `origin/lesson-60` side (`id: string` / `findOne(id)`) before any of the seven applied diffs began. Because `origin/lesson-60` is only the diff's base commit (never itself diffed against anything upstream of it in this task's range), that fix was never expressed as a hunk in any of the seven `origin/lesson-60..67` diffs — it was already "baked in" to a starting snapshot that this task's differently-sourced local `lesson-51` (forked from the `lesson-18` checkpoint per the lesson-51 row above, not from any `origin/lesson-59`-adjacent commit) did not share. `git diff --stat lesson-58 origin/lesson-67 -- src` confirms this is the *only* remaining `src/` divergence from the original upstream MongoDB path's final state. This mirrors, in shape, the informational `git diff lesson-50 origin/lesson-59` check the SQL-lineage report ran at the equivalent boundary (line 145 above) — but unlike that check (which found only expected, inert drift), this one surfaces a live-breaking gap.

**Fixed** (ruled a real spec gap, not out-of-scope, since lesson-55's own HTML mandates the change): amended `lesson-55`'s commit (`867b959` → `6eb6ed7`) to `findOne(@Param('id') id: string) { return this.coffeesService.findOne(id); }`, verified the amendment touches only `coffees.controller.ts` (plus expected `tsconfig.build.tsbuildinfo` churn), then rebased `lesson-56` → `288a387`, `lesson-57` → `e87696d`, and `lesson-58` → `9234681` on top in sequence (`git rebase --onto`). Only one rebase conflict occurred, in the generated `tsconfig.build.tsbuildinfo` (resolved by taking the incoming version — it regenerates on every build regardless); `coffees.controller.ts`/`.service.ts` auto-merged cleanly in every step since none of lessons 56–58's own diffs touched `findOne`. `rebuild/mongodb` now points at the new `lesson-58`. Re-verified: `npm run build`/`npm run lint` exit 0 on all four new SHAs; live repro against a fresh `mongo:8.0.4` container — `POST /coffees` → `201`, `GET /coffees/<real _id>` → **`200`** (previously `500`) — confirmed on both the fixed `lesson-55` and the final `lesson-58`. `git diff` from each pre-fix SHA to its corresponding new SHA shows only the controller fix, nothing else lost or altered.

**MongoDB image fallback confirmed live:** `docker run mongo:8.0` failed to start with the exact documented error (`Linux kernel versions 6.19 and newer has a known incompatibility with this version of MongoDB`, `SERVER-121912`); `docker run mongo:8.0.4` started cleanly. This matches the HTML's own documented guidance at lesson-52 (which also names `mongo:8.0.4` as the tested fallback) — used only as an uncommitted local container for this task's own verification; `docker-compose.yml` itself still correctly pins `mongo:8.0` as committed.

**Transactions behavior confirmed live (lesson-57):** against the standalone (non-replica-set) `mongo:8.0.4` container — matching the topology `docker-compose.yml` actually configures — `POST /coffees/:id/recommend` did not throw at `session.startTransaction()` but did fail on the first session-attached write, exactly as `57_use-transactions-mongodb.html` (line 90) predicts for a standalone container; `mongosh` confirmed the target coffee's `recommendations` field remained `0` (the write was correctly aborted, not partially applied), matching the HTML's stated "all-or-nothing" proof point.

**Housekeeping found and cleaned up:** a leftover `node --enable-source-maps dist/main` process (PID 53986, started before this session, `cwd` = the companion clone) was still running and listening on port 3000 from the crashed prior implementer's own local testing — not mentioned in the task handoff (only a leftover Docker container was). It was intercepting this task's own curl-based verification traffic (returning plausible-looking 2xx responses from a different lesson's build) until identified via `lsof -i :3000` / `ps -p <pid>` and killed; verification was then re-run cleanly. The `mongo-verify-task7` container created for this task's own live testing was removed at the end (`docker rm -f`); final state: `docker ps` shows only the four pre-existing, unrelated containers (`ecommerce-api-db-1`, `iluvcofee-db-1`, `mongo-rs`, `mongo-standalone`), port 3000 is free, and `git status --short` in the companion clone is empty.

**Isolation checks (Step 3 of the brief):** all passed — `@nestjs/mongoose` present in `lesson-58`'s `package.json`/`src` only; `@prisma/client` absent from `lesson-58`; `src/events/entities/event.entity.ts` exists on `lesson-58` and is absent from `lesson-50`; `prisma/schema.prisma` exists on `lesson-50`.

Evidence: `/private/tmp/nestjs-companion-sync-20260917/repo` (branches `lesson-52`..`lesson-58`, `rebuild/mongodb`); live verification via a locally-run `mongo:8.0.4` container (`mongo-verify-task7`, removed) and `node dist/main.js` (`npm run build` output) against `curl`/`mongosh`.

## Pre-publication verification

Independent final local verification gate (Task 8), run against the isolated companion clone at `/private/tmp/nestjs-companion-sync-20260917/repo` (Node `v26.7.0`, npm `11.19.0`). Confirms the local ref set produced by Tasks 1–7 is ready to publish; does not push or delete any remote ref, and does not modify any lesson branch, `main`, or `end-of-chapter-2`.

**Step 1 — target ref inventory:**

```bash
cd /private/tmp/nestjs-companion-sync-20260917/repo
for n in $(seq -w 1 58); do git show-ref --verify --quiet "refs/heads/lesson-$n" || git show-ref --verify --quiet "refs/remotes/origin/lesson-$n" || exit 1; done
test "$(git rev-parse main^{tree})" = "$(git rev-parse lesson-50^{tree})"
test "$(git rev-parse origin/lesson-18^{tree})" = "$(git rev-parse origin/end-of-chapter-2^{tree})"
```

All 58 `lesson-NN` local heads exist (no fallback to `origin/*` needed). `main^{tree}` == `lesson-50^{tree}` == `c03958bdfaa46ba54d5e4daf32393af213ca2d16` — **PASS**. `origin/lesson-18^{tree}` == `origin/end-of-chapter-2^{tree}` == `df96d051fccf9af14e9dffb0e25c08ad35ca82c1` (informational check on the old frozen origin refs) — **PASS**. Additional explicit local checkpoint check (the ref Task 9 actually publishes): `test "$(git rev-parse end-of-chapter-2)" = "$(git rev-parse lesson-18)"` — **PASS**, both `8dd9cc1de97f80da852068630becec80e1d5e700`.

**Step 2 — structural course checks**, run in the course repository:

```bash
git diff --check
```

Exit 0 — **PASS**. No prior audit script/methodology for the sequential-filename/index/navigation/id/tag checks was found in this repo's history (checked `git log --oneline -- Week_09/01_nestjs_basic/content` and the "renumber to 67 lessons" commit `fd23950`), so the following independent checks were run directly against `Week_09/01_nestjs_basic/content/`:

- `ls | grep -E '^[0-9]{2}_.*\.html$' | wc -l` → **58**; filenames are sequentially numbered `01`–`58` with no gaps or duplicates (verified by listing).
- Index membership: every one of the 58 content filenames appears as an `href` inside `index.html` — **PASS**, 0 missing. Explicit count: `grep -oE 'href="[0-9]{2}_[^"]+\.html"' index.html | sort -u | wc -l` → **58**, exactly matching the content directory.
- Previous/next navigation: a Python check parsed each file's `rel="prev"`/`rel="next"` anchors and confirmed lesson 01 has no `prev`, lesson 58 has no `next`, and every other file's `prev`/`next` hrefs exactly match its immediate neighbors in the 01–58 sequence — **0 errors** across all 58 files.
- Unique HTML IDs: no file has a duplicate `id="..."` attribute within itself — **0 files with duplicates**.
- Local link resolution: every non-`http(s)`/`#`/`mailto:` `href` across all 58 lesson files plus `index.html` resolves to an existing file on disk — **0 broken links**.
- Balanced major tags: `tidy -q -e` run against all 58 files, filtered for `unclosed|not closed|mismatched|missing.*</` — **0 files with tag issues**.

**Step 3 — branch-to-content review** (local `lesson-$n` used uniformly for all 58 lessons, per the correction to the brief):

- Corrected row-count command: `grep -c '^| lesson-[0-9][0-9] ' docs/superpowers/reports/2026-09-17-nestjs-companion-repo-sync.md` → **58** — matches expected.
- Gap/duplicate check: extracted all `lesson-NN` row numbers, sorted, and diffed against `seq -w 1 58` → **exact match, no gaps or duplicates**.
- Every one of the 58 rows has a title, a source-state commit SHA, a `PASS` content result, and a build/test result column present (visually confirmed by re-reading the full Branch audit table, rows for lesson-01 through lesson-58).
- Independent spot-checks (claims not previously verified in this task, chosen across both the SQL and MongoDB ranges), each confirmed against the LOCAL branch tree via `git show lesson-NN:path` and cross-checked against the current HTML:
  - **lesson-29** (SQL, transactions): `git show lesson-29:src/coffees/coffees.service.ts` contains `this.prisma.$transaction(async (tx) => {` — confirms the row's "transaction callback" claim.
  - **lesson-44** (SQL, Swagger model decorating): `git show lesson-44:src/coffees/dto/create-coffee.dto.ts` has `@ApiProperty({ description: 'The name of a coffee.' })`/`@ApiProperty({ description: 'The brand of a coffee.' })`/`@ApiProperty({ example: ['chocolate', 'vanilla'] })`; `update-coffee.dto.ts` imports `PartialType` from `@nestjs/swagger` (not `@nestjs/mapped-types`) — confirms both halves of the row's claim. Cross-checked against `44_decorating-model-properties.html`, which shows the identical `@ApiProperty` code block.
  - **lesson-53** (MongoDB, Mongoose module): `git show lesson-53:package.json` has `"@nestjs/mongoose": "^12.0.0"` and `"mongoose": "^9.10.0"`; `git show lesson-53:src/app.module.ts` has `MongooseModule.forRoot('mongodb://localhost:27017/nest-course')` alongside `CoffeesModule` — confirms the row's claim.
  - **lesson-57** (MongoDB, transactions): `git show lesson-57:src/events/entities/event.entity.ts` has `@Prop({ type: mongoose.SchemaTypes.Mixed })` (braced form) on `payload`; `git show lesson-57:src/coffees/coffees.service.ts` has `import type { Connection, Model } from 'mongoose'`, `@InjectConnection() private readonly connection: Connection`, and `startSession()`/`startTransaction()`/`commitTransaction()`/`abortTransaction()` calls — confirms the row's claim. Cross-checked against `57_use-transactions-mongodb.html`, which documents the same `Mixed` requirement.

No discrepancies found in any spot-check.

**Step 4 — final companion repository checks:**

`lesson-50` (full SQL path, commit `a154103709aa1b1323c3d3197865c231c20b47da`):

All exit codes below were re-measured with the command's own exit status captured directly into a variable immediately after the command (`cmd > log 2>&1; echo $?`), not through a piped `tail` (an initial pass mistakenly captured `tail`'s exit status instead of the command's; every command was re-run to get the real code, and every result was unchanged — see raw logs under this session's scratchpad `task8-logs/`).

- `npm ci` — exit 0 (511 packages).
- `npm run build` — initially failed with TS2339/TS2307 errors (missing generated Prisma client, since `npm ci`'s postinstall scripts are not run under this environment's `allowScripts` policy); ran `npx prisma generate --config prisma7.config.ts` (exit 0, generated to `./src/generated/prisma`), then `npm run build` — exit 0, clean.
- `npm run lint` — exit 0; 1 pre-existing warning (`parse-int.pipe.ts` unused `metadata` param), matching the row-58/lesson-40 documented warning.
- `npm test` — exit 0; 4 test files, 6 tests passed, matching the report's row 68.
- e2e lifecycle, three runs against the port-5433 `test-db` (only the pre-existing unrelated containers `ecommerce-api-db-1`, `iluvcofee-db-1`, `mongo-rs`, `mongo-standalone` were ever running alongside it; the task's own `repo-test-db-1` was created/removed three times, cleanly each time):
  1. `npm run pretest:e2e` — exit 0 (container created, 3 migrations applied). `npm run test:e2e` — exit **1**: `test/app.e2e-spec.ts` failed (`Config validation error: DATABASE_URL: "DATABASE_URL" is required`) because the clone has no `.env` and the full `AppModule` requires `DATABASE_URL`/`API_KEY` at boot (the same harness/environment gap Task 5 documented and worked around, see line 141 above — not a content defect); `test/coffees/coffees.e2e-spec.ts` (which sets `process.env.DATABASE_URL` itself) passed all 5 of its own tests, including creating coffee id `1` and then deleting it. Since the main script failed, npm's `posttest:e2e` did not auto-fire and the container was left running with its id sequence already advanced past `1`.
  2. Created a temporary, untracked `.env` (`DATABASE_URL` pointing at the same `test-db`, an `API_KEY` value, `PORT=3000`). Re-ran `npm run test:e2e` against the **same still-running container** from run 1 — exit **1** again, but for a different reason: `app.e2e-spec.ts` now passed (DATABASE_URL/API_KEY present), but 3 of `coffees.e2e-spec.ts`'s 5 tests failed (`Get one [GET /:id]` → 404 "Coffee #1 not found", `Update one` → `undefined` name, `Delete one` → 404) because the new `POST /coffees` in this run received id `2` (the container's sequence had already advanced in run 1), while the spec hardcodes `/coffees/1`. This is a **test/environment non-idempotency**, not a rebuild defect: `test/coffees/coffees.e2e-spec.ts`'s five cases assume a fresh, empty `test-db` and are not safe to re-run against a container that already has data — worth flagging for Task 10, which must always run a fresh `pretest:e2e` immediately before `test:e2e`, never reuse a container across runs.
  3. Ran `npm run posttest:e2e` explicitly — exit 0, removed the stale container. Ran `npm run pretest:e2e` — exit 0 (fresh container, 3 migrations applied). Ran `npm run test:e2e` (which itself auto-chains `pretest:e2e`/`posttest:e2e` via npm's `pre`/`post` script convention since the main script this time succeeded) — exit **0**, **2 test files, 6 tests passed**; `posttest:e2e` auto-fired and removed the container. Deleted the temporary `.env` afterward.
- `git status --short --ignored` after all lesson-50 checks: only ignored `dist/`, `node_modules/`, `src/generated/`, `tsconfig.build.tsbuildinfo` — no tracked changes.

`lesson-58` (MongoDB path, build+lint only, commit `92346819b77cc64d27f220cc3776f93af46d37f3` = `rebuild/mongodb`):

- Before starting: found an untracked `src/generated/` directory left over from switching from `lesson-50` (this lineage's `.gitignore` does not list `src/generated`, unlike the SQL lineage; it is not part of this branch's tree — `git ls-files | grep -c '^src/generated'` = 0). Removed it (`rm -rf src/generated`) before proceeding, since it is not part of the MongoDB lineage. (This carryover reproduced identically on a second branch switch during exit-code re-verification, confirming it is deterministic branch-switch behavior in a shared working tree, not a one-off.)
- `npm ci` — exit 0 (no Prisma involved on this lineage).
- `npm run build` — exit 0, clean.
- `npm run lint` — exit 0; 1 pre-existing warning (`coffees.service.ts:72` unused `err` catch parameter), matching the row-76/lesson-58 documented warning.
- No live MongoDB test/e2e run performed, per the brief and this task's instructions (already verified live in Task 7).
- `git restore tsconfig.build.tsbuildinfo` — restored the one tracked file the build touched (this lineage tracks it, unlike the SQL lineage from lesson-32 onward).
- `git clean -fdX` — removed ignored `dist/` and `node_modules/`.
- Final `git status --short` — **empty, 0 lines**. Working tree fully clean.

**Step 5 — ref graph review:**

```bash
git log --graph --decorate --oneline --all --simplify-by-decoration
git for-each-ref --sort=refname --format='%(refname:short) %(objectname) %(tree)' refs/heads/lesson-* refs/heads/main
```

Graph confirms: `lesson-01`..`lesson-50`/`main`/`rebuild/sql` form one continuous line from the shared `iluvcoffee` scaffold; `lesson-51`..`lesson-58`/`rebuild/mongodb` branch off the shared `lesson-18`/`end-of-chapter-2` checkpoint (`8dd9cc1`) as a separate line, never touching the SQL lesson-19–50 line; the old `origin/lesson-*` refs (including `84aca79`, the old configuration-namespace commit) sit on an entirely disconnected historical graph with no edges into any local `lesson-NN` ref. `git for-each-ref` printed commit+tree OIDs for all 58 local `lesson-*` refs plus `main`, matching the SHAs recorded in each Branch audit row above (spot-checked: `lesson-50`/`main` both `a154103...`/tree `c03958b...`; `lesson-58` `9234681...`/tree `79e934d...`).

Ancestor check:

```bash
for n in $(seq -w 36 50); do git merge-base --is-ancestor 84aca79 "lesson-$n" && exit 1 || true; done
```

Loop completed without hitting `exit 1` for any of `lesson-36`–`lesson-50` — **PASS**, none of the rebuilt SQL refs descend from the old namespace commit.

**Concerns:** none rising to a real content or build defect. Three harmless environmental/informational notes, all resolved within this task: (1) the isolated clone has no `.env`, so a temporary untracked one was needed to run `lesson-50`'s full e2e suite (same gap Task 5 already documented); (2) a stray untracked `src/generated/` directory carried over when switching from `lesson-50` to `lesson-58` in the same working tree, removed before building since it is not part of the MongoDB lineage's tracked or ignored content; (3) informational, for Task 10: `test/coffees/coffees.e2e-spec.ts` hardcodes id `1` and is only valid against a genuinely fresh `test-db` container — re-running `test:e2e` against a container that already has data from a prior run fails on 404s unrelated to any code defect, so any future verification must always pair a fresh `pretest:e2e` immediately before `test:e2e`.

**Result: all 58 lesson refs, `main`, and `end-of-chapter-2` are verified consistent, buildable, and ready for Task 9 to publish.**

## Published remote state

**Published.** Remote (`git@github-variden:VaridenTech/varislab-intro-to-nestjs-v1.git`) now holds exactly 60 refs: `main`, `end-of-chapter-2`, and `lesson-01` through `lesson-58`. `lesson-59` through `lesson-67` were deleted after full verification. Verified via `git ls-remote --heads origin` after publication: 60 lines, no `lesson-59`–`lesson-67` remaining, every one of `lesson-01`–`lesson-58` matches its verified local target OID, `main` matches local `lesson-50`, and `end-of-chapter-2` matches the local corrected checkpoint (not the stale old `origin/end-of-chapter-2`).

Per Task 2's binding downstream ruling, the full range `lesson-01`–`lesson-58` was republished (not only `lesson-33`–`lesson-58` as the original task brief said) because Task 2's remediation rebuilt the entire local `lesson-01`–`lesson-32` lineage (project name, README, Prisma placement, lesson-18 fix); `end-of-chapter-2` was republished for the same reason.

Representative final OIDs:

| Ref | Final OID |
| --- | --- |
| `lesson-01` | `c61e5f46b2ea88f6c86a47665b7a1968d0b28897` |
| `lesson-22` | `68635ab083310f80acb0943ff50cefaf95c841ab` |
| `lesson-32` | `130aeeb54b3fa2cb951be299fb9ddaec35be2772` |
| `lesson-33` | `98231e925b901433e64e3ae90d808a6fa21941b4` |
| `lesson-50` | `a154103709aa1b1323c3d3197865c231c20b47da` |
| `lesson-51` | `1e91f92e69e118822e0e21add7d7e60b55dfff50` |
| `lesson-58` | `92346819b77cc64d27f220cc3776f93af46d37f3` |
| `main` | `a154103709aa1b1323c3d3197865c231c20b47da` (= `lesson-50`) |
| `end-of-chapter-2` | `8dd9cc1de97f80da852068630becec80e1d5e700` (= `lesson-18`) |

Publication was performed across several sessions, all with explicit user confirmation before any push: an automated dispatch published `lesson-01` through `lesson-32` (32/60 refs) using exact `--force-with-lease` leases against the Task 1 frozen pre-sync OIDs; the remaining 26 lesson pushes plus `main` and `end-of-chapter-2` were blocked twice by this environment's auto-mode safety classifier on an unrelated permission-scoping issue (not a real concurrency or safety problem — remote state was independently re-verified clean and unchanged after each block), so the user ran those 28 `--force-with-lease` pushes directly; the 9 obsolete-branch deletions (`lesson-59`–`lesson-67`) were then run and each independently verified. A full `git ls-remote` comparison after every phase confirmed no unexpected concurrent remote changes at any point (matched the Task 1 `pre-sync-refs.txt` snapshot throughout, until each ref's own intended update).
